import react from 'react';
// import "./Video.css"

export default function Video({ videoSrc }){
  
  <Modal
  show={isModalOpen}
  onClose={handleCloseModal}
  title="Video Player" 
  selectedVideo={selectedVideo}
>
  <Video videoSrc={selectedVideo} />
</Modal>
}